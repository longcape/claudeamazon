# エージェントホーム最上位資料への追記用

以下を、最上位の管理資料（プロジェクト一覧・現在地が分かる資料）へ
**1エントリとして追記または更新**してください。細かい情報はコピーしないこと。

---

### 楽天ROOM運用エンジン

| 項目 | 内容 |
| --- | --- |
| 現在の段階 | **実装完了・実データ未検証 / ローカル環境へ移行中** |
| 参照先 | 該当部屋の `START-HERE.md`（運用手順）/ `HANDOFF.md`（技術引継ぎ） |
| コード | GitHub `longcape/claudeamazon` ブランチ `claude/rakuten-room-automation-8c13zi` の `rakuten-room/` |
| 次にやること | 移行7ステップの進捗確認 → `probe` で実データ点検 → 商材ジャンルの決定 |
| 最終更新 | 2026-09-01 |
| 注意 | 楽天APIを一度も叩いていない。検証はすべて擬似データ。実データ点検を最優先 |

---

## 記載時の判断メモ

- 既に同名または類似のエントリがある場合は、**新規追加せず上書き更新**する
- 「現在の段階」「次にやること」「最終更新」の3項目だけでも更新されていれば、
  どのAIが入っても現在地を把握できる
- 詳細は部屋側の資料に置き、最上位には持ち込まない
