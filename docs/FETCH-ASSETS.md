# 公式画像を取り込む手順

> **公式画像はリポジトリに同梱済みです**（`assets/img/` に 159 枚）。
> clone や ZIP のダウンロードをした時点で、エージェントの顔・スキルアイコン・
> マップのミニマップ・スパイクはすべて表示されます。
> **この手順が要るのは、新エージェントや新マップが出て取り直すときだけです。**

エージェントのアイコンとマップを、Riot の公式画像に差し替える（取り直す）手順です。
**フォルダ移動やコマンドの暗記は要りません。** ファイルを画面にドラッグするだけです。

所要時間はおよそ 15 分（うち Node.js のインストールが 5 分）。

---

## 全体の流れ

```
[1] Node.js を入れる     … 1 回だけ。次回以降は不要
[2] コードをダウンロード  … ZIP を展開するだけ
[3] コマンドを 2 回実行   … ドラッグ＆ドロップで入力
[4] 確認                … index.html を開く
[5] （任意）GitHub に戻す … オンライン版にも反映したい場合
```

---

## [1] Node.js を入れる

https://nodejs.org/ja を開き、**「推奨版（LTS）」**をダウンロードします。

インストーラーを起動したら、あとは **「次へ」→「次へ」→「インストール」** で終わりです。
設定を変更する必要はありません。

> すでに入っている場合はこの手順を飛ばしてください。バージョンが古いと
> ツールが「Node.js 18 以上が必要です」と教えてくれるので、その時に入れ直せば大丈夫です。

---

## [2] コードをダウンロードする

次のリンクを開くと ZIP のダウンロードが始まります。

https://github.com/longcape/claudeamazon/archive/refs/heads/claude/valorant-tactical-setup-card-iiiog3.zip

> **なぜ普通のダウンロードではダメか**
> このリポジトリはコードが作業ブランチにしか入っておらず、
> トップページから普通にダウンロードすると中身が空になります。
> 上のリンクはブランチを指定済みなので、そのまま使ってください。

ダウンロードした ZIP を**展開**します。

- **Windows** — ZIP を右クリック →「すべて展開」
- **Mac** — ZIP をダブルクリック

展開先はデスクトップなど、後で見つけやすい場所にしてください。
`claudeamazon-claude-valorant-tactical-setup-card-iiiog3` という長い名前のフォルダができます。

その中を開くと、こうなっていれば成功です。

```
├── index.html      ← これが見えていれば正解
├── build.js
├── assets/
├── docs/
└── tools/
```

---

## [3] 実行する

### いちばん簡単な方法（推奨）

展開したフォルダの中にある **`画像を取得.bat`**（Mac は `画像を取得.command`）を
**ダブルクリック**するだけです。黒い画面が開いて自動で進みます。

- Node.js が入っていなければ、その場で案内が出ます
- 画像を縮小する `sharp` も自動で導入されます（初回のみ）
- 取得と `build.js` の実行までまとめて終わります

> Windows で「WindowsによってPCが保護されました」と出た場合は、
> 「詳細情報」→「実行」を押してください。自分で用意したファイルなので問題ありません。

終わったら `index.html` をダブルクリックして確認してください。

### 手動で実行する場合

以下は上の方法がうまくいかないときだけ読んでください。

> **注意**: `fetch-assets.mjs` を**ダブルクリックしないでください。**
> Windows が「どのアプリで開きますか」と聞いてきます。`.mjs` は
> 開くファイルではなく、node に渡して実行するファイルです。

## コマンドを手で打つ

### 3-1. 黒い画面を開く

- **Windows** — キーボードの `Windowsキー` を押し、`cmd` と入力して Enter
- **Mac** — `command + スペース` を押し、`ターミナル` と入力して Enter

どこで開いても構いません。フォルダを移動する必要はありません。

### 3-2. 1 つ目のコマンド

開いた画面に、**半角で `node` と入力し、そのあとに半角スペースを 1 つ**入れます。

```
node 
```

次に、さきほど展開したフォルダの中の `tools` フォルダを開き、
**`fetch-assets.mjs` というファイルを、黒い画面の中へドラッグ＆ドロップ**します。

ファイルの場所が自動で入力されます。こうなります。

```
node C:\Users\あなた\Desktop\claudeamazon-...\tools\fetch-assets.mjs
```

この状態で **Enter** を押します。

数十秒かかります。次のように表示されれば成功です。

```
data.js のエージェント 29 体 / マップ 13 種を対象にします
  agent  jett       28 KB
  agent  phoenix    31 KB
  ...
  map    ascent     412 KB
  ...
✓ 29 体のアイコン / 13 枚のミニマップ / 116 個のスキルアイコン / スパイク 1 枚を取り込みました
```

### 3-3. 2 つ目のコマンド

同じ要領で、今度は `build.js` をドラッグします。

```
node 
```
と入力してから、フォルダ直下の **`build.js`** をドラッグ＆ドロップして **Enter**。

```
dist/valorant-tactical-setup-card.html  ○○○ KB
dist/artifact-body.html                 ○○○ KB
```

と出れば完了です。

> `node build.js` を実行すると、画像は自動的に単一 HTML へ埋め込まれます。
> 相対パスのままでは配布ファイルで画像が表示されないためです。

---

## [4] 確認する

フォルダの中の **`index.html` をダブルクリック**してください。
ブラウザでアプリが開きます。

エージェント選択画面（味方スロットをクリック）を開いて、**顔写真が出ていれば成功です。**
（画像が取れていない場合だけ、六角形のアイコンで代替表示されます。）

新エージェントを取り込んだ場合は、そのエージェントの顔とスキルアイコンが
出ているかを確認してください。

スパイクは `assets\img\spike.png` があれば取得できています。
配置盤（戦術カードの「▣ 配置を編集」）を開いて、ツールバー左端の
「プラント位置」チップのアイコンが公式のものになっていれば反映されています。

---

## [5] （任意）オンライン版にも反映する

手順 [4] までで、**あなたの PC 上のアプリ**は公式画像になります。
GitHub 上のコードや共有用のページにも反映したい場合は、生成されたファイルをアップロードします。

アップロードするのは次の 5 つです。

```
assets/img/agents/       ← 29 個の PNG
assets/img/abilities/    ← 116 個の PNG（スキルアイコン）
assets/img/maps/         ← 13 個の PNG
assets/img/spike.png     ← スパイク
assets/js/official-assets.js
```

前回から増えたぶんだけで構いません。
同じ内容のファイルを上げ直しても問題は起きません。

**GitHub の画面から入れる方法**（コマンド不要）

1. https://github.com/longcape/claudeamazon を開く
2. 画面上部のブランチ選択で `claude/valorant-tactical-setup-card-iiiog3` を選ぶ
3. **Add file** → **Upload files** をクリック
4. 上の 5 つをドラッグ＆ドロップ（フォルダごとドラッグできます）
5. 下部の **Commit changes** を押す

---

## うまくいかないとき

| 表示されたもの | 対処 |
| --- | --- |
| `'node' は、内部コマンドまたは外部コマンド...` | Node.js が入っていません。手順 [1] へ |
| `Node.js 18 以上が必要です` | Node.js が古いです。https://nodejs.org/ja から入れ直してください |
| `valorant-api.com に接続できませんでした` | 会社や学校の回線だと遮断されていることがあります。自宅の回線かスマホのテザリングで試してください |
| `Cannot find module` | ドラッグしたファイルが違います。`tools` フォルダの中の `fetch-assets.mjs` か確認してください |
| 画像が変わらない | 手順 [3-3] の `build.js` を実行し忘れていないか確認してください。ブラウザの再読み込み（`Ctrl + F5`）も試してください |
| `spike 取得できませんでした` | スパイクだけ取れなかった状態です。アプリは自前の図で動きます。URL が分かる場合は `node tools\fetch-assets.mjs --spike-url=<URL>` で指定できます |

---

## このツールが何をしているか

1. `assets/js/data.js` からエージェントとマップの一覧を読む
2. valorant-api.com（Riot の公式アセットを配信しているコミュニティのミラー）から画像を取得し `assets/img/` に保存
3. `assets/js/official-assets.js` を書き出し、アプリが公式画像を使うようにする

取得するもの: エージェントのアイコン / スキルのアイコン / マップのミニマップ /
**スパイクのアイコン**（`assets/img/spike.png`）。

スパイクはエージェントやマップとは別の場所にあるため、ゲームモード
「スタンダード」の表示アイコンから取っています。API 側の名称が変わって
見つけられなかった場合は、URL を直接指定して取り込めます。

```
node tools/fetch-assets.mjs --spike-url=<画像のURL>
```

スパイクを取り込めていない間は、アプリが自前で描いた図を代わりに表示します。
配置盤の「プラント位置」はどちらの場合でも同じように使えます。

**おまけ**: アプリに未登録のエージェントが API 側にあると警告します。
新エージェント実装時の取りこぼしを、このツールで検出できます。

```
⚠ data.js に未登録のエージェントが API 側にあります:
   ○○ (デュエリスト)
```

---

## 画像の利用について

Riot は「Legal Jibber Jabber」で二次利用を認めており、**収益化も条件つきで許可**しています。
**無料公開なら事前の申請や個別の許可は不要**です。

| 条件 | 内容 |
| --- | --- |
| 表示 | **Riot が指定した英文**の免責表記を見やすい場所に掲示する。自分の言葉で書き直すのは不可 |
| 誤認 | 公式・公認と誤解させない（プロダクト名やドメイン、ロゴの扱い） |
| 権利 | Riot がそのプロジェクトを利用・改変・配布できることを受け入れる |
| 登録（収益化時） | [Developer Portal](https://developer.riotgames.com/) に製品登録し、**Approved** の状態にする |
| 無料枠（収益化時） | プレイヤー向けの無料枠が必須。**そこに広告を入れることは明示的に許可されている** |
| 課金（収益化時） | コンテンツが transformative（新しい洞察を付加したもの）であること |
| 禁止 | 賭博・ベッティング、Riot のプロパティ内への広告掲載 |

本アプリの相性判定と AI 寸評は、構成から新しい洞察を生成しているため
transformative の定義に当てはまります。

指定文言の実物と、収益化に踏み込む場合の詳細は **[LEGAL.md](LEGAL.md)** にまとめてあります。

なお本アプリは**プレイヤーの試合データを一切取得していません**（すべて手入力）。
そのため Riot Sign On (RSO) による同意フローは不要で、要件は静的アセットの利用のみです。
