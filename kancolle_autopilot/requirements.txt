# Phase 1 は標準ライブラリのみで動作する。
# 実行時依存はまだ無い（PyAutoGUI / discord.py / LLM SDK は Phase 5 以降）。

# 開発・テスト用
pytest>=8.0

# LLM によるタスク解釈を使う場合のみ（main.py plan --text）
# anthropic>=1.0

# Discord から管理コマンドを受ける場合のみ（main.py run --discord）
# discord.py>=2.3
