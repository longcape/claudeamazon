# VALORANT TACTICAL SETUP CARD

VALORANT 競技シーン向けの **ラウンド戦術セットアップカード**。
試合前に戦術を登録し、ラウンドの勝敗に応じて次のラウンドの戦術を選び直していく
「戦術コールの管理ツール」です。tracker.gg / valorant.gg のタクティカルボード風のダーク UI。

ビルド不要の静的アプリ（HTML + CSS + Vanilla JS、依存パッケージなし）。
`index.html` をブラウザで開くだけで動きます。

- **相性判定** — 味方と相手のエージェント構成から、その戦術が刺さるかを判定
- **AI 寸評** — Claude が構成を読んで弱点と対策を書く（任意設定）
- **コミュニティ投稿** — 「僕の考えた最強の戦術」を共有・取り込み（任意設定）
- **X へ投稿** — 戦術カードをワンクリックでポスト
- **多言語** — 日本語 / English / 한국어（言語ファイル 1 枚で追加可能）
- **全 29 エージェント対応** — ヴィトー（2025/10/08）とミクス（2026/03/19）を含む最新ロスター
- **マップ簡易図** — 全 13 マップのサイト位置関係を図示し、戦術のターゲットをハイライト
- **配置盤** — マップ上にエージェントとスキルをドラッグで配置し、進行ルートを矢印で描く。スキルには使用順の番号が付く

---

## 使い方

### 01. SETUP — 試合の準備

| 項目 | 内容 |
| --- | --- |
| **MATCH SETUP** | マップ（全 13 マップ）、チーム名、前半のサイド（ATTACK / DEFENSE）、スカウトメモ |
| **AGENT COMPOSITION** | 味方 5 / 敵 5 のエージェントをセット。ロール別フィルタ・検索・プレイヤー名（IGN）付き |
| **TACTIC DECK** | ラウンドで使う戦術を事前登録。戦術名 / 使用サイド / ターゲットサイト / 戦術タイプ / コール詳細 |

味方・敵のエージェントが 5 体ずつ揃い、戦術が 1 つ以上登録されると **START MATCH** が押せます。

### 02. LIVE BOARD — ラウンドループ

```
戦術を選択  →  カードを表示（コール共有）  →  WIN / LOSS を記録  →  次ラウンドの戦術を選択  →  …
```

1. **戦術を選択**
   現在のサイドで使える戦術だけが候補に並び、直前ラウンドの結果をもとに **SCORE（推奨度 0-100）** と
   推奨理由が表示されます。エコノミー（FULL BUY / FORCE / ECO / BONUS）も併せて記録できます。
2. **戦術カード**
   選んだ戦術が大きく表示されます。サイト・戦術タイプ・コール詳細・その戦術の通算成績・連投数が一目で分かります。
3. **WIN / LOSS**
   ラウンド結果を押すと記録され、自動で次ラウンドの戦術選択に戻ります。
   サイドは 13 ラウンド目で自動反転、25 ラウンド目以降（OT）は毎ラウンド交代します（手動反転も可）。

下段には **ROUND TIMELINE**（全ラウンドの戦術と勝敗）と **TACTIC PERFORMANCE**（戦術別勝率）が並びます。

---

## 配置盤（TACTICAL BOARD）

戦術ごとに、マップ上へエージェントとスキルを配置できます。配置は戦術に紐づいて保存され、
その戦術を選ぶと復元されます。

| できること | 操作 |
| --- | --- |
| エージェントを置く | 左右のパレットで選び、マップをタップ |
| スキルを置く | 同上。スロット（C / Q / E / X）単位で置ける |
| 位置を動かす | 置いたものをドラッグ |
| 使用順を決める | スキルを置くと自動で採番。選択して「順番を早く / 遅く」で入れ替え |
| 進行ルートを描く | 「ルートを描く」→ マップを順にタップ →「ルート確定」 |

**スキルの使用順**は、オーメンのように同時に 1 つしか展開できないアビリティで効きます。
番号は**エージェントごとに 1 から**振られ、ライブボードにはエージェント別の一覧が出ます。

```
JETT   ① クラウドバースト   ② クラウドバースト   ③ テイルウィンド
OMEN   ① ダークカバー
```

表示サイズは配置盤のツールバーから 5 段階（320px 〜 700px）で変更でき、設定は記憶されます。
スキルアイコンとアビリティ名は `tools/fetch-assets.mjs` を実行すると公式のものになります
（アビリティ名は日本語 / 英語 / 韓国語をまとめて取得します）。

アビリティの正式名称とアイコンは `tools/fetch-assets.mjs` を実行すると公式のものになります
（名称は日本語 / 英語 / 韓国語をまとめて取得します）。

配置盤は **SETUP の戦術カードの「▣ 配置を編集」**、戦術の編集画面、
そして LIVE BOARD の配置盤パネルから開けます。試合前に用意しておけます。

> 同時展開数（チャージ）は表示していません。パッチごとに変わるうえ公式 API では
> 配信されておらず、手書きで持つと必ず古くなるためです（ネオンのリレーボルトは
> パッチ 8.11 で 2 個から 1 個に変更されています）。
> 置いた数と使用順で意図は伝わるため、誤った数を出すより表示しない方を選んでいます。

---

## 相性判定（MATCHUP ANALYSIS）

戦術を選ぶと、**味方 5 体 × 相手 5 体 × マップ × 戦術タイプ**から相性を判定します。
`刺さる` / `五分` / `刺さらない` の判定と、その根拠が一件ずつ並びます。

エージェントごとに「何ができるか」をタグ（スモーク・フラッシュ・索敵・地点排除・設置物・
機動力・アビリティ無効化など）として持たせ、構成単位で集計してルールに当てています。

例: **ジェット / スカイ / オーメン / キルジョイ / ゲッコー** で
**レイズ / ソーヴァ / ヴァイパー / サイファー / ブリーチ** に B ファストラッシュを仕掛けた場合

```
刺さらない  32/100
  通る根拠   フラッシュ 3 枚で一気に距離を詰められる
  弱点       VIPER の壁で進行を止められる
             相手の設置物 1 個で速攻が止まる
             相手の地点排除 4 枚で、入り口で足を止められる
             相手の定点ユーティリティ 2 枚が設置後に効いてくる
```

判定は**完全にオフラインで動き、通信もコストも発生しません**。実戦中に使うのはこちらです。

### AI 寸評（任意）

上記に加えて、Claude（`claude-opus-5`）に文章での寸評を書かせられます。
API キーは Edge Function に置くのでブラウザには露出しません。
1 人 1 日あたりの生成回数に上限があり、青天井の課金は発生しない作りです。
設定手順とコストの試算は **[docs/SETUP.md](docs/SETUP.md)** を参照してください。

---

## 推奨ロジック（SCORE の出し方）

基準値 50 点に対して、以下を加減算します。

| 条件 | スコア |
| --- | --- |
| 通算勝率 | `(勝率 - 50) × 0.7` |
| 未使用（相手に情報を与えていない） | **+14** |
| 直前ラウンドで成功した戦術 | **+10** |
| 直前ラウンドで失敗した戦術 | **-22** |
| 同じ戦術の連投（n 連投） | **-8 × n** |
| 直近 3 ラウンド以内に使用 | **-6** |
| 6 ラウンド以上使っていない | **+8** |
| 直前が敗北 + ターゲットサイトを変更 | **+7** |
| 直前が敗北 + フェイク / スプリット系 | **+4** |
| サイド不一致（「他サイドも表示」時） | **-30** |

「勝った戦術は相手が対応してくるまで回す / 負けた戦術の連投は避ける / 連投は読まれる」という
実戦のセオリーをそのままスコア化しています。**あくまで判断材料**なので、最終的なコールは自由に選べます。

---

## その他の機能

- **自動保存** — 編成・戦術デッキ・ラウンド履歴は `localStorage` に保存され、リロードしても復元されます
- **EXPORT / IMPORT** — 設定と戦績を JSON で書き出し / 読み込み（チーム内での共有やマップ別デッキの使い分けに）
- **直前のラウンドを取り消す** — 押し間違えた結果を 1 つ戻す
- **スコアリセット** — 編成と戦術デッキを残したままスコアだけ初期化
- **キーボードショートカット** — 戦術カード表示中は `W` = WIN / `L` = LOSS、戦術選択中は `1`〜`9` で候補を選択
- **レスポンシブ** — PC / タブレット / スマホに対応（試合中はスマホでの確認を想定）
- **X へ投稿** — 戦術名・マップ・サイド・相性判定をまとめてポスト（API キー不要）
- **多言語** — 日本語 / English / 한국어。初回はブラウザの言語設定に自動で合わせます
- **コミュニティ投稿** — 戦術を共有し、他の人の戦術を自分のデッキに取り込めます
- **登録数の上限** — `config.js` の `TACTIC_LIMIT_FREE` で設定。0 なら無制限（既定）
- **マッチ決着時** — 勝敗バナーに「新しいマッチを始める」ボタンが出ます

---

## 起動方法

```bash
# そのまま開く
open index.html

# もしくは簡易サーバー経由で
npx http-server . -p 8080
# → http://localhost:8080
```

## 配布用ビルド

全ファイルを 1 枚の HTML に束ねます（依存パッケージなし）。

```bash
node build.js
```

| 生成物 | 用途 |
| --- | --- |
| `dist/valorant-tactical-setup-card.html` | スタンドアロン版。このファイル 1 つを配ればよく、ダブルクリックで起動、オフラインでも動作 |
| `dist/artifact-body.html` | 公開ページ発行用（外側の `html` / `head` / `body` は発行側で付与される） |

EXPORT ボタンは、通常のブラウザでは Blob 経由でダウンロードし、
公開ページ上では閲覧者に確認を出してから保存します（`downloads` capability）。

## エージェントとマップの画像

エージェントアイコンは既定では**エージェントごとに固有色を割り当てた六角アイコン**（外周のリングがロール色）で描画します。
ロール色だけだとデュエリスト 8 体がすべて同じ赤になり見分けられないためです。

マップは**簡易図**を内蔵しています。実寸のミニマップではなく、
「サイトがどの位置関係にあるか」「今の戦術がどこを狙うか」を戦術ボード上で即座に読むためのものです。

### 公式画像に差し替える

Riot の公式アセットを取り込むツールを同梱しています。

```bash
node tools/fetch-assets.mjs            # 画像ファイルとして保存（推奨）
node tools/fetch-assets.mjs --inline   # 単一 HTML 配布版にも埋め込む場合
node build.js                          # 配布ファイルに反映
```

ターミナルの使い方から順を追った手順は **[docs/FETCH-ASSETS.md](docs/FETCH-ASSETS.md)** にまとめてあります。

`assets/js/official-assets.js` が生成され、以降はエージェントアイコンと
マップのミニマップが公式画像に差し替わります。
このツールは **API 側に存在してアプリに未登録のエージェントも報告する**ので、
新エージェントが追加されたときの取りこぼし検出にも使えます。

> **画像の権利について**
> 取得される画像は Riot Games の著作物です。ファンプロジェクトでの利用は
> Riot の "Legal Jibber Jabber" ポリシーに従ってください。
> 広告や課金を伴う配信を予定している場合は、事前に利用条件の確認を推奨します。

## ファイル構成

```
index.html                      画面のマークアップ
assets/css/style.css            スタイル（タクティカルボード風のダークテーマ）
assets/js/config.js             Supabase / AI 寸評の接続設定（空でも動く）
assets/js/i18n.js               翻訳の登録・切り替え
assets/js/locales/*.js          言語ごとの辞書（ja / en / ko）
assets/js/data.js               エージェント / マップ / 戦術タイプのマスタデータ
assets/js/agent-traits.js       エージェントの戦術的な特性タグ
assets/js/portraits.js          エージェント固有色と公式画像の参照層
assets/js/maps-layout.js        13 マップの簡易図
assets/js/official-assets.js    公式画像の差し込み（自動生成）
assets/js/store.js              状態管理・localStorage 永続化・スコアやサイドの算出
assets/js/advisor.js            次ラウンドの戦術推奨ロジック
assets/js/analyst.js            構成の相性判定エンジン
assets/js/abilities.js          エージェントのアビリティ（スロット / 同時展開数）
assets/js/board.js              配置盤のデータ操作と描画
assets/js/share.js              X への投稿・テキストのコピー
assets/js/community.js          Supabase 連携（REST 直叩き / SDK 不要）
assets/js/ui.js                 描画処理
assets/js/app.js                イベント登録・画面遷移
build.js                        単一 HTML へのバンドル
dist/                           ビルド生成物
supabase/schema.sql             テーブル・RLS ポリシー・RPC
supabase/functions/tactic-review/  AI 寸評を返す Edge Function
CLAUDE.md                       開発メモ（設計の前提と過去の失敗）
docs/SETUP.md                   コミュニティ / AI 寸評のセットアップと課金の考え方
docs/FETCH-ASSETS.md            公式画像の取得手順
docs/HANDOFF.md                 ローカル環境で開発を続けるための手順
tools/fetch-assets.mjs          Riot 公式アセットの取得ツール
tools/smoke-test.mjs            配布ファイルの動作確認
```

## 開発する

```bash
git clone -b claude/valorant-tactical-setup-card-iiiog3 \
  https://github.com/longcape/claudeamazon.git
```

`index.html` をブラウザで開けばそのまま動きます。ビルドは不要です。
`assets/` を触ったら `node build.js` で `dist/` を作り直してください。

```bash
node build.js            # 単一 HTML を作り直す
node tools/smoke-test.mjs   # 動作確認（要 playwright）
```

設計の前提と、過去に踏んだ地雷は **[CLAUDE.md](CLAUDE.md)** にまとめてあります。
自分の PC で開発を続ける手順は **[docs/HANDOFF.md](docs/HANDOFF.md)** を参照してください。

## エージェントロスター（29 体）

| ロール | エージェント |
| --- | --- |
| デュエリスト (8) | ジェット / フェニックス / レイズ / レイナ / ヨル / ネオン / アイソ / ウェイレイ |
| イニシエーター (7) | ソーヴァ / ブリーチ / スカイ / ケイオー / フェイド / ゲッコー / テホ |
| コントローラー (7) | ブリムストーン / オーメン / ヴァイパー / アストラ / ハーバー / クローヴ / **ミクス** |
| センチネル (7) | キルジョイ / サイファー / セージ / チェンバー / デッドロック / ヴァイス / **ヴィトー** |

相性判定は各エージェントの特性タグを見ているので、
新エージェントを `data.js` と `agent-traits.js` に追加すれば判定にも自動で反映されます。

## マップ（13 種）

アセント / バインド / ヘイヴン / スプリット / アイスボックス / ブリーズ / フラクチャー /
パール / ロータス / サンセット / アビス / コロード / **サミット**

サミット（2026-06-24 実装）は横に広い 3 レーン構成で、A・ミッド・B に
**展開可能な壁**があります。簡易図では壁の位置を金色の破線で示し、
「ミッドの攻防が勝敗を分ける」「決め打ちの進行が壁で崩れうる」という
マップ固有の判定を相性エンジンに入れてあります。

> 本アプリは非公式のファンメイドツールです。Riot Games とは関係ありません。
